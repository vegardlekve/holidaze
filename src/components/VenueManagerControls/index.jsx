import React from "react";
import styled from "styled-components";

const VenueManagerControls = ({ user, token }) => {
    const handleCreateVenue = () => {};

    const handleUpdateVenue = (venueId) => {};

    const handleDeleteVenue = (venueId) => {};

    const handleViewBookings = (venueId) => {};

    return (
        <ManagerSection>
            <h2>Manage Your Venues</h2>
            {}
        </ManagerSection>
    );
};

export default VenueManagerControls;

const ManagerSection = styled.section``;
